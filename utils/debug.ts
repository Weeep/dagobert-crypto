export const r = (logable: any, num: number = 0) => {
  console.log(`rrrrr ${num !== 0 ? num + " " : ""}` + JSON.stringify(logable));
};

export const q = (logable: any, num: number = 0) => {
  console.log(`qqqqq ${num !== 0 ? num + " " : ""}` + JSON.stringify(logable));
};

export const a = (logable: any, num: number = 0) => {
  console.log(`aaaaa ${num !== 0 ? num + " " : ""}` + JSON.stringify(logable));
};

export const b = (logable: any, num: number = 0) => {
  console.log(`bbbbb ${num !== 0 ? num + " " : ""}` + JSON.stringify(logable));
};

export const c = (logable: any, num: number = 0) => {
  console.log(`ccccc ${num !== 0 ? num + " " : ""}` + JSON.stringify(logable));
};
