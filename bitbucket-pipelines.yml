#  Template NodeJS build

#  This template allows you to validate your NodeJS code.
#  The workflow allows running tests and code linting on the default branch.

image: node:22

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - node
            script:
              - npm install && npm run build && pm2 restart app || pm2 start "npm start" --name app
        - step:
            name: Code linting
            script:
              - npm install eslint
              - npx eslint .
            caches:
              - node
# CHAT GPT
# image: node:20

# pipelines:
#   default:
#     - step:
#         name: Build & Deploy to Lightsail
#         script:
#           - apt-get update && apt-get install -y openssh-client
#           - echo "$PRIVATE_KEY" > key.pem
#           - chmod 600 key.pem
#           - ssh -o StrictHostKeyChecking=no -i key.pem ubuntu@YOUR_SERVER_IP '
#               cd /home/ubuntu/your-app &&
#               git pull &&
#               npm install &&
#               npm run build &&
#               pm2 restart app || pm2 start "npm start" --name app
#           '
